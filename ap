public class Solution {
    public static int missingNumber(int []a, int N) {
      int sum=(N * (N + 1)) / 2;
       int s2=0;
       for(int i=0;i<N-1;i++){
           s2+=a[i];
       }
       return (sum-s2);
       
          }
}
